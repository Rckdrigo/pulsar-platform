# Satellite MCP Server Configuration

# Environment mode
# Set to 'development' for dev environment, 'production' for Docker
NODE_ENV=development

# Enable debug logging for MCP server
# Set to "mcp:*" to enable all debug output
DEBUG=

# Transport Configuration
# Options: "stdio" (default, for Claude Desktop) or "http" (for web clients)
TRANSPORT_MODE=stdio

# HTTP Server Configuration (only used when TRANSPORT_MODE=http)
# Port for HTTP streaming transport
HTTP_PORT=8000

# MCP Server Authentication
# API key required to access the MCP server via HTTP transport
# Leave empty to disable authentication (development only)
MCP_API_KEY=your-secure-mcp-api-key-here

# Pulsar API Configuration
# For local development: use localhost:3000
# For Docker Compose: use http://pulsar-backend:3000
PULSAR_SOCKET_URL=http://localhost:3000

# Optional API key for Pulsar authentication
PULSAR_API_KEY=your-api-key-here
